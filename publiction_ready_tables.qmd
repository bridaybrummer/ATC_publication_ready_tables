---
title: "publication_ready_tables"
format: docx
editor: visual
reference-doc: "Extras/NMC_reference.docx"
---

## Running Quarto

Quarto is a next generation version of Rmakrdown. It can handle multiple programming language and is slight faster. Some instances do not handle outputs to word documents as well as Rmarkdown.

## Running Code

Generally YAML is slightly difference and code chunks are in lower case. Knitr can still setup some of the defaults.

```{r, setup}
#| echo: false

library(gtsummary)
library(flextable)

```

You can add options to executable code like this

```{r}
#| echo: false
#| label: tbl-trial
#| tbl-cap: The caption for this is specified in the code chunk

trial %>%tbl_summary(by = trt) %>%as_flex_table
```

Look at @tbl-trial.

The `echo: false` option disables the printing of code (only output is displayed).

Need to update quarto with development release:\
